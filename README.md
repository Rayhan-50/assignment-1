## ১. 🔑 What is the use of the keyof keyword in TypeScript? Provide an example.


টাইপস্ক্রিপ্ট (TypeScript) ডেভেলপারদের জন্য *টাইপ নিরাপত্তা (Type Safety)* নিশ্চিত করার একটি অত্যন্ত গুরুত্বপূর্ণ টুল হলো keyof অপারেটর। এটি একটি অবজেক্ট টাইপের প্রপার্টি নাম বা চাবিগুলিকে (keys) টাইপ হিসাবে ব্যবহার করার সুযোগ দেয়, যা জেনারেটিক (Generic) ফাংশন এবং ইউটিলিটি টাইপস লেখার ক্ষেত্রে অপরিহার্য।

### 🤔 keyof কি এবং কেন এটি দরকার?

keyof হলো এমন একটি *প্রকারভেদ নির্দেশক (type operator), যা একটি অবজেক্ট টাইপ থেকে তার সমস্ত **সর্বজনীন প্রপার্টি নামগুলির* আক্ষরিক টাইপগুলো একত্রিত করে একটি *ইউনিয়ন টাইপ (Union Type)* গঠন করে। মূলত, এটি অবজেক্টটির কাঠামোতে ব্যবহৃত প্রপার্টির নামগুলিকে প্রকার হিসেবে নিষ্কাশন করে আনে।

> উদাহরণস্বরূপ, যদি আপনার কাছে { a: number, b: string } টাইপের একটি অবজেক্ট থাকে, তবে keyof সেই অবজেক্টে 'a' | 'b' ইউনিয়ন টাইপটি তৈরি করবে।

### 🎯 keyof এর মূল ব্যবহার (Type Safety নিশ্চিত করা)

keyof-এর প্রাথমিক ব্যবহার হলো এটি নিশ্চিত করা যে আপনি একটি প্রদত্ত অবজেক্ট টাইপে *আসলেই বিদ্যমান* প্রপার্টিগুলিই কেবল অ্যাক্সেস করছেন। এটি *জেনারেটিক ফাংশন (Generic Functions)*-কে কম্পাইল টাইমেই টাইপ-নিরাপদ করে তোলে।

### 💡 উদাহরণ: টাইপ-নিরাপদ প্রপার্টি গেটার তৈরি করা

আসুন একটি সাধারণ পরিস্থিতিতে দেখি: একটি অবজেক্ট থেকে একটি প্রপার্টির মান পাওয়ার জন্য একটি *ইউটিলিটি ফাংশন* তৈরি করা।

#### ১. অবজেক্ট টাইপ সংজ্ঞায়িত করা

typescript
interface UserProfile {
  id: number;
  username: string;
  email: string;
  isVerified: boolean;
}

// keyof UserProfile === 'id' | 'username' | 'email' | 'isVerified'
`

#### ২. keyof সহ জেনারেটিক গেটার ফাংশন

typescript
/**
 * @param obj - যে অবজেক্ট থেকে প্রপার্টির মান নেওয়া হবে (টাইপ T)।
 * @param key - অবজেক্টের বৈধ কী (টাইপ K, যা keyof T দ্বারা সীমাবদ্ধ)।
 * @returns প্রপার্টির মান (টাইপ T[K], যা লুকআপ টাইপ ব্যবহার করে সংজ্ঞায়িত)।
 */
function getPropertyValue<T extends object, K extends keyof T>(
  obj: T,
  key: K
): T[K] { 
  return obj[key];
}


#### ৩. বাস্তবে কার্যকারিতা প্রদর্শন

typescript
const myProfile: UserProfile = {
  id: 123,
  username: "ts_dev",
  email: "dev@example.com",
  isVerified: true
};

// ✅ সঠিক কী ব্যবহার: এটি সফলভাবে কাজ করে এবং টাইপ সুরক্ষিত থাকে।
const userName = getPropertyValue(myProfile, 'username'); 

// ❌ ভুল কী ব্যবহার: এটি একটি কম্পাইলার এরর ঘটায়!
// Argument of type '"age"' is not assignable to parameter of type '"id" | "username" | "email" | "isVerified"'.
// কম্পাইলার জানে যে 'age' UserProfile-এর একটি বৈধ কী নয়।
// const userAge = getPropertyValue(myProfile, 'age'); 


keyof ব্যবহার করার ফলে, কম্পাইলার স্বয়ংক্রিয়ভাবে 'age' আর্গুমেন্টটিকে অবৈধ ঘোষণা করে, কারণ এটি UserProfile-এর বৈধ কী-এর ইউনিয়ন টাইপ ('id' | 'username' | 'email' | 'isVerified') এর অন্তর্ভুক্ত নয়।

-----

## ২. 🏷 What is the use of enums in TypeScript? Provide an example of a numeric and string enum.


টাইপস্ক্রিপ্টে **enum** হলো একটি শক্তিশালী ডেটা টাইপ যা নির্দিষ্ট *ধ্রুবকগুলির (constants)* একটি সেটকে *অর্থবহ এবং সহজে মনে রাখা যায় এমন নাম* দিতে সাহায্য করে। এটি আপনার কোডকে অত্যন্ত *সহজে পঠনযোগ্য (readable)* এবং *টাইপ-নিরাপদ (type-safe)* করে তোলে।

সাধারণত, যে সকল ক্ষেত্রে সম্ভাব্য মানগুলির সংখ্যা সীমিত (যেমন: লগ লেভেল, সপ্তাহের দিন, ব্যবহারকারীর ভূমিকা বা স্ট্যাটাস কোড), সেখানেই enum ব্যবহার করা হয়।

### 🎯 enum ব্যবহারের মূল সুবিধা

| সুবিধা | বিবরণ |
| :--- | :--- |
| *পঠনযোগ্যতা বৃদ্ধি* | সাধারণ সংখ্যা বা স্ট্রিং-এর পরিবর্তে আপনি অর্থপূর্ণ নাম ব্যবহার করতে পারেন। |
| *টাইপ নিরাপত্তা* | এটি নিশ্চিত করে যে আপনি কেবল সেই নির্দিষ্ট সেট থেকে অনুমোদিত মানগুলিই ব্যবহার করছেন। |
| *স্বয়ংক্রিয় ডকুমেন্টেশন* | enum নিজেই কী কী সম্ভাব্য মান রয়েছে তা বর্ণনা করে। |

### 🔢 নিউমেরিক (Numeric) enum উদাহরণ

টাইপস্ক্রিপ্টে **enum** ডিফল্টভাবে নিউমেরিক হয় (০ দিয়ে শুরু)।

#### উদাহরণ: অ্যাপ্লিকেশন স্ট্যাটাস ট্র্যাক করা

typescript
// ডিফল্টভাবে, Pending = 0, Active = 1, Archived = 2
enum Status {
  Pending, 
  Active,
  Archived,
}

const currentStatus: Status = Status.Active; // currentStatus এর মান হবে 1

// ব্যবহার ও আউটপুট
if (currentStatus === Status.Active) {
  console.log("ব্যবহারকারী সক্রিয়।"); // আউটপুট: ব্যবহারকারী সক্রিয়।
}

console.log(`পেন্ডিং স্ট্যাটাসের নিউমেরিক মান: ${Status.Pending}`); // আউটপুট: 0


### 📝 স্ট্রিং (String) enum উদাহরণ

স্ট্রিং enum ডিব্যাগিং-এর সময় স্পষ্টতা দেওয়ায় জনপ্রিয়। এক্ষেত্রে প্রতিটি সদস্যকে একটি স্ট্রিং মান দিয়ে স্পষ্ট করে শুরু করা *বাধ্যতামূলক*।

#### উদাহরণ: নির্দিষ্ট রঙের সেট ব্যবহার করা

typescript
enum Color {
  Red = "RED_HEX_CODE",
  Green = "GREEN_HEX_CODE",
  Blue = "BLUE_HEX_CODE",
}

const chosenColor: Color = Color.Green; // chosenColor এর মান হলো "GREEN_HEX_CODE"

// ব্যবহার
function applyTheme(colorCode: Color) {
  console.log(`থিমের জন্য নির্বাচিত রং: ${colorCode}`);
}

applyTheme(chosenColor); // আউটপুট: থিমের জন্য নির্বাচিত রং: GREEN_HEX_CODE
// applyTheme("YELLOW"); // ❌ কম্পাইলার এরর! 


```
```
