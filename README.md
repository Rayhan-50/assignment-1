## টাইপস্ক্রিপ্টে enum এর ব্যবহার কী? 🏷

টাইপস্ক্রিপ্টে **enum** (সংক্ষিপ্ত রূপ: enumerations) হলো এমন একটি বিশেষ ডেটা টাইপ যা নির্দিষ্ট *ধ্রুবকগুলির (constants)* একটি সেটকে অর্থবহ নাম দিতে সাহায্য করে। এটি আপনার কোডকে আরও *সহজে পঠনযোগ্য (readable)* এবং *টাইপ-নিরাপদ (type-safe)* করে তোলে।

এগুলো সাধারণত এমন ক্ষেত্রেই ব্যবহৃত হয় যেখানে আপনার একটি নির্দিষ্ট সংখ্যক সম্ভাব্য মান (যেমন: সপ্তাহের দিন, ট্র্যাফিকের আলোর রং, বা স্ট্যাটাস কোড) থাকতে পারে।

### 🎯 enum ব্যবহারের মূল সুবিধা

  * *পঠনযোগ্যতা বৃদ্ধি:* সংখ্যা বা সাধারণ স্ট্রিং ব্যবহার না করে, আপনি অর্থপূর্ণ নাম ব্যবহার করতে পারেন। যেমন, const status = 0; লেখার চেয়ে const status = Status.Pending; লেখা অনেক বেশি বোধগম্য।
  * *টাইপ নিরাপত্তা:* enum ব্যবহার করার মাধ্যমে টাইপস্ক্রিপ্ট কম্পাইলার নিশ্চিত করে যে আপনি কেবল সেই নির্দিষ্ট সেট থেকে অনুমোদিত মানগুলিই ব্যবহার করছেন।
  * *ডকুমেন্টেশন:* enum নিজেই কী কী সম্ভাব্য মান রয়েছে তা বর্ণনা করে কোডের একটি অংশ হিসেবে কাজ করে।

-----

## 🔢 নিউমেরিক (Numeric) enum উদাহরণ

ডিফল্টভাবে, টাইপস্ক্রিপ্টের enum নিউমেরিক হয়। প্রথম মানটি *০ (শূন্য)* দিয়ে শুরু হয় এবং এর পরের মানগুলি ক্রমানুসারে বাড়তে থাকে। আপনি চাইলে প্রথমটির জন্য একটি নির্দিষ্ট মান নির্ধারণ করতে পারেন।

### উদাহরণ

ধরুন আপনি একটি অ্যাপ্লিকেশন ডেটার স্ট্যাটাস ট্র্যাক করতে চান:

```typescript
// ডিফল্টভাবে, Pending = 0, Active = 1, Archived = 2
enum Status {
  Pending, 
  Active,
  Archived,
}

// আমরা প্রথম মানটিকে ১ থেকে শুরু করতে পারি:
/*
enum Status {
  Pending = 1, // এখন Pending = 1, Active = 2, Archived = 3
  Active,
  Archived,
}
*/

const currentStatus: Status = Status.Active; // এখন currentStatus এর মান হবে 1

// ব্যবহার
if (currentStatus === Status.Active) {
  console.log("ব্যবহারকারী সক্রিয়।");
}

console.log(পেন্ডিং স্ট্যাটাসের নিউমেরিক মান: ${Status.Pending}); // আউটপুট: 0
console.log(নিউমেরিক মান 1 এর জন্য নাম: ${Status[1]}); // আউটপুট: Active


⸻

📝 স্ট্রিং (String) enum উদাহরণ

নিউমেরিক enum-এর চেয়ে স্ট্রিং enum আজকাল বেশি জনপ্রিয়, কারণ এটি ডিব্যাগিং-এর সময় বেশি বোধগম্য স্ট্রিং মান প্রদান করে এবং টাইপ-নিরাপত্তা বজায় রাখে। স্ট্রিং enum-এর ক্ষেত্রে, প্রতিটি সদস্যকে একটি স্ট্রিং মান দিয়ে স্পষ্ট করে শুরু করতে বাধ্যতামূলক।

উদাহরণ

ধরুন আপনি একটি নির্দিষ্ট রং-এর সেট ব্যবহার করতে চান:

enum Color {
  Red = "RED_HEX_CODE",
  Green = "GREEN_HEX_CODE",
  Blue = "BLUE_HEX_CODE",
}

const chosenColor: Color = Color.Green; // chosenColor এর মান হলো "GREEN_HEX_CODE"

// ব্যবহার
function applyTheme(colorCode: Color) {
  console.log(থিমের জন্য নির্বাচিত রং: ${colorCode});
}

applyTheme(chosenColor); // আউটপুট: থিমের জন্য নির্বাচিত রং: GREEN_HEX_CODE
applyTheme(Color.Blue); // বৈধ
// applyTheme("YELLOW"); // ❌ কম্পাইলার এরর, কারণ "YELLOW" Color enum-এর অংশ নয়
